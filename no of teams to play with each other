def number_of_teams(M):
     
    N1, N2, sqr = 0,0,0
 
    sqr = math.sqrt(1 + (8 * M))
 
    N1 = (1 + sqr) / 2

    N2 = (1 - sqr) / 2
 
    if (N1 > 0):
        return int(N1)
    return int(N2)


#test function

def test_teams():

  assert(math.sqrt(1 + (9 * M))
