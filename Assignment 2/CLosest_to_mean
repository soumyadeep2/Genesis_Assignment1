 distance = Math.abs(array[0] - mean)
     i = 0
    for  c in c < array.length 
    {
         cdistance = Math.abs(array[c] - mean)
        if (cdistance < distance)
        {
            i = c
            distance = cdistance
        }
    }
